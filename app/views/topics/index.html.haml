%h1 Topics

.row
  .col-md-8
    - @topics.each do |topic|
      .media
        .media-body
          .media-heading
            = link_to topic.name, topic
          %small
            = link_to topic.description
  .col-md-4
    - if policy(Topic.new).create?
      = link_to "New Topic", new_topic_path, class: 'btn btn-success'
= will_paginate @topics
