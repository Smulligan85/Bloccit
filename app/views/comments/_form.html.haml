



= form_for [post, comment] do |f|
  - if post.errors.any?
    .alert.alert-danger
      %h4 There are #{pluralize(comment.errors.count, "errors")}
      %ul
        - comment.errors.full_messages.each do |msg|
          %li= msg
  = form_group_tag(comment.errors[:body]) do
    = f.label :body
    = f.text_area :body, rows: 4, class: 'form-control', placeholder: "Enter comment body"

  = f.submit "Create Comment", class: 'btn btn-success'